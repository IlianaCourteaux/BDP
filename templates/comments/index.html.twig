{% set username = comment.user.username %}

<div id="comment-{{ comment.id }}">
        <div class="comment_item">
            
            <h5 class="comment_title">
                <span class="comment-author">{{ username }}</span>
            </h5>
            
            <p class="comment_date">{{ comment.createdAt|date('d M Y à H:i') }}</p>

            <p class="comment_message">{{ comment.text }}</p>

        </div>
</div>
